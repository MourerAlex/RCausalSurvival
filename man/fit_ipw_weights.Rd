% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_ipw.R
\name{fit_ipw_weights}
\alias{fit_ipw_weights}
\title{Build IPW Weights}
\usage{
fit_ipw_weights(
  pt_data,
  id_col,
  treatment_col,
  covariates_vec,
  formulas,
  ipcw,
  stabilize,
  truncate
)
}
\value{
List with
\itemize{
\item \code{pt_data} — augmented with weight columns.
\item \code{models}  — list of fitted glms (\code{A}, \code{A_num}, \code{c}, \code{c_num}).
\item \code{checks}  — list of per-model diagnostics.
\item \code{flagged_ids} — ids of subjects with weights flagged by
truncation (or \code{NULL} when no truncation requested).
}
}
\description{
Compute the inverse-probability weight for each person-time row,
\deqn{W_i(k) \;=\; \frac{1}{\pi(A_i \mid L_i)} \,
                  \prod_{j=1}^{k}\frac{1}{1 - h_C(j;\, A_i, L_i)},}
replacing each factor by its stabilized ratio when
\code{stabilize = "marginal"}:
}
\details{
\tabular{lll}{
   \code{stabilize} \tab propensity numerator \tab censoring numerator \cr
   \code{"marginal"} \tab \code{A ~ 1} \tab \code{cond_indep_cens ~ A} (when ipcw) \cr
   NULL \tab none (unstabilized) \tab none (unstabilized) \cr
}


Truncate at the requested percentile and attach \code{w_a}, \code{w_cens},
\code{w_combined} (and their raw counterparts) to \code{pt_data}. Shared by
\code{\link[=fit_ipw_msm]{fit_ipw_msm()}} and \code{\link[=fit_ipw_km]{fit_ipw_km()}}; the engines differ only in the
downstream survival-estimation step. The censoring numerator is
fixed to \code{cond_indep_cens ~ A} (H&R Technical Point 12.2).
}
\keyword{internal}
