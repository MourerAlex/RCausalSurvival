% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_survival.R
\name{causal_survival}
\alias{causal_survival}
\title{Counterfactual risk under a binary point treatment}
\usage{
causal_survival(
  pt_data,
  method = "gformula",
  formulas = NULL,
  truncate = NULL,
  ipcw = NULL,
  stabilize = "marginal",
  verbose = FALSE,
  keep_data = TRUE,
  .ipw_estimator = "km"
)
}
\arguments{
\item{pt_data}{A \code{person_time} object returned by
\code{\link[=to_person_time]{to_person_time()}}.}

\item{method}{Estimator. One of \code{"gformula"}, \code{"ipw"}, or \code{"km"}.}

\item{formulas}{Optional named list of model formula overrides.
Keys: \code{y} (Y-hazard), \code{c} (C-hazard / IPCW denominator), \code{A}
(propensity denominator), \code{A_num} (propensity numerator). Any
absent key falls back to the default linear formula.}

\item{truncate}{\code{NULL} or length-2 numeric \code{c(lower, upper)}
percentile bounds for IPW weight truncation. \code{NULL} leaves the
weights untruncated.}

\item{ipcw}{\code{NULL} or logical. \code{NULL} resolves to the
method-conditional default (\code{TRUE} under \code{"ipw"}, \code{FALSE} under
\code{"gformula"}).}

\item{stabilize}{One of \code{"marginal"} or \code{NULL}. Drives both the
treatment and the censoring numerators; v1 supports marginal
stabilization only.}

\item{verbose}{Logical.}

\item{keep_data}{Logical. When \code{TRUE}, the \code{pt_data} (and the
subject-level input it was built from) are retained on the
returned fit.}

\item{.ipw_estimator}{Internal. Survival-curve estimator under
\code{method = "ipw"}: \code{"km"} (default, weighted pooled-hazard Kaplan-Meier,
nonparametric in \code{k}) or \code{"msm"} (weighted pooled logistic with
the cubic-in-\code{k} default). The leading dot flags this as a
developer-facing knob.}
}
\value{
An S3 object of class \code{"causal_survival_fit"}.
}
\description{
Estimate the counterfactual risk
\eqn{E[Y_k^{a, c = 0}] = \Pr(Y_k^{a, c = 0} = 1)} that the event of
interest has occurred by the end of discrete interval \code{k} under
arm \code{a} of a binary treatment assigned at baseline (\code{t = 0}) and
held fixed thereafter, with the treatment-dependent censoring
component held at zero. Equivalently, the cumulative incidence
\eqn{F^a(t_k) = \Pr(T^a \le t_k, C^{d,a} = 0)}; the survival
\eqn{S^a(t_k) = 1 - F^a(t_k)} follows. Time is discretized into a
finite grid of intervals \verb{k = 1, …, K_max} under the half-open
convention \verb{(t_\{k-1\}, t_k]}.
}
\details{
Identification rests on consistency, exchangeability conditional
on the recorded baseline covariates \code{L_0}, and positivity in both
the treatment and the censoring mechanisms. Censoring is split
into a treatment-dependent component \code{cond_indep_cens} (the component
\code{c} set to zero in the estimand) and an administrative or
otherwise independent component \code{indep_cens} (handled by the
at-risk set, not by intervention).

Two estimators are exposed:
\itemize{
\item \code{method = "gformula"}: discrete-time parametric g-formula. Fit
the discrete hazards of \code{Y} (and, when needed, of \code{C}) on the
pooled person-time data, then simulate the counterfactual risk
\verb{E[Y_k^\{a, c = 0\}]} in each arm by recursive substitution.
\item \code{method = "ipw"}: inverse-probability weighting. Build the
product weight (treatment and, when applicable, censoring) and
estimate the counterfactual hazard in each arm by a weighted
pooled-hazard Kaplan-Meier estimator (default, nonparametric in \code{k}) or by a
weighted pooled-logistic marginal structural model
(\code{.ipw_estimator = "msm"}); the risk is recovered from the
hazards.
\item \code{method = "km"}: unadjusted Kaplan-Meier per arm. No propensity
model is fit (any covariates on \code{pt_data} are silently ignored).
Optionally accepts \code{ipcw = TRUE} to correct for treatment-
conditional dependent censoring via inverse-probability weights.
Identifies the marginal counterfactual under MARGINAL
exchangeability (e.g., randomization) and independent censoring
(or conditional-on-treatment independent censoring when
\code{ipcw = TRUE}).
}

Standard errors are not produced by this function; obtain them by
bootstrap on the subject-level input.
}
