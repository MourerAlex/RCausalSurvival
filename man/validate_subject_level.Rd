% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_subject_level}
\alias{validate_subject_level}
\title{Validate Subject-Level Input}
\usage{
validate_subject_level(
  data,
  id,
  time,
  status,
  treatment,
  covariates,
  cut_points,
  time_varying
)
}
\arguments{
\item{data}{A data.frame in subject-level format.}

\item{id, time, status, treatment}{Character. Column names.}

\item{covariates}{Character vector of baseline covariate column names.}

\item{cut_points}{\code{NULL}, a length-1 positive integer (number of
equi-spaced intervals), or a numeric vector of length >= 2 (explicit
interior cut points strictly within \verb{(0, max(time))}).}

\item{time_varying}{Reserved for future use. Must be \code{NULL} in v1.}
}
\value{
Invisibly \code{TRUE} on success.
}
\description{
Checks subject-level data (one row per subject) before person-time
construction in \code{\link[=to_person_time]{to_person_time()}}. Hard-errors on structural problems
that would corrupt the long-format output or downstream estimation.
}
\details{
\subsection{Hard errors}{
\itemize{
\item Missing required columns
\item \code{id}: NAs or duplicates (one row per subject)
\item \code{time}: not numeric, negative, or NAs
\item \code{status}: not in \verb{\{0, 1\}}, or NAs
\item \code{treatment}: NAs, fewer than 2 levels, or more than 2 levels
\item covariates: NAs or unsupported types (via \code{\link[=check_covariate_quality]{check_covariate_quality()}})
\item \code{cut_points}: invalid form, non-integer scalar, non-increasing
vector, or values outside \verb{(0, max(time))}
\item \code{time_varying != NULL}
}
}
}
\keyword{internal}
