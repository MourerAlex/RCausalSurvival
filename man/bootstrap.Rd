% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap}
\alias{bootstrap}
\alias{bootstrap.default}
\alias{bootstrap.causal_survival_fit}
\title{Bootstrap confidence bands}
\usage{
bootstrap(fit, ...)

\method{bootstrap}{default}(fit, ...)

\method{bootstrap}{causal_survival_fit}(fit, n_boot = 500, alpha = 0.05, seed = NULL, ...)
}
\arguments{
\item{fit}{A fitted object. For this package: a
\code{"causal_survival_fit"} from \code{\link[=causal_survival]{causal_survival()}}.}

\item{...}{Passed on to methods.}

\item{n_boot}{Positive integer. Number of bootstrap replicates.}

\item{alpha}{Two-sided significance level in \verb{(0, 1)}.}

\item{seed}{Optional integer RNG seed for reproducibility.}
}
\value{
An S3 object of class \code{"causal_survival_bootstrap"} with
the shape documented in spec §4.3.
}
\description{
S3 generic. Subject-level percentile bootstrap pairing a fit with
confidence bands. CausalSurvival ships the
\code{"causal_survival_fit"} method; CausalCompetingRisks registers a
method for its \code{"causal_competing_risks_fit"} fits, so \code{bootstrap()}
dispatches correctly whichever package masks the other.
}
\details{
Each replicate samples unique subject IDs with replacement, stitches
the corresponding person-time rows back together with synthetic IDs,
and re-evaluates \code{fit$call} with the resampled \code{pt_data} (spec
line 767). Failed replicates (errors during refit) are tracked in
\verb{$failed_reps}; the effective B is \code{n_boot - length(failed_reps)}.
}
