% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_km.R
\name{fit_km}
\alias{fit_km}
\title{Unadjusted Kaplan-Meier per arm (with optional IPCW)}
\usage{
fit_km(
  pt_data,
  id_col,
  treatment_col,
  cut_times,
  formulas,
  ipcw,
  stabilize,
  truncate
)
}
\description{
Discrete-time Kaplan-Meier per arm. For each arm \verb{a in \{0, 1\}}, filter
to the at-risk rows and compute the pooled-hazard
\deqn{\hat\lambda^a_k \;=\;
  \frac{\sum_i W_i\, 1\{Y_{ik}=1,\, A_i = a\}}
       {\sum_i W_i\, 1\{\text{at risk at } k,\, A_i = a\}},
  \qquad
  \hat F^a(k) \;=\; 1 - \prod_{j=1}^{k}(1 - \hat\lambda^a_j).}
Under \code{ipcw = FALSE} the weights \code{W_i} are uniform (1), yielding the
standard discrete-time KM. Under \code{ipcw = TRUE} the weights are the
inverse-probability-of-censoring weights \code{w_cens} from a fitted
C-hazard model (\code{cond_indep_cens ~ A + k + I(k^2) + I(k^3)} by default).
}
\details{
KM is marginal in baseline \code{L_0}: no propensity model is fit and any
covariates carried on \code{pt_data} are ignored. The C-hazard model under
\code{ipcw = TRUE} is also marginal in \code{L_0} (treatment + time only).
Identifies the marginal counterfactual under marginal exchangeability
(e.g., randomization) and either independent censoring
(\code{ipcw = FALSE}) or no-unmeasured-confounders-of-censoring beyond \code{A}
(\code{ipcw = TRUE}).

Requires baseline treatment. v1 restricts the package to a binary
baseline \code{A}.
}
\keyword{internal}
