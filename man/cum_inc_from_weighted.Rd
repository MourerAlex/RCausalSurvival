% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazards.R
\name{cum_inc_from_weighted}
\alias{cum_inc_from_weighted}
\title{Cumulative Incidence from Weighted Person-Time (Single-Event)}
\usage{
cum_inc_from_weighted(y_event, k, weights, cut_times)
}
\arguments{
\item{y_event}{Numeric vector of Y event indicators. See
\code{\link[=weighted_hazard_by_k]{weighted_hazard_by_k()}} for the at-risk encoding.}

\item{k}{Integer vector of interval indices (1..K_max under spec
§3.0.2). Used as the grouping key for the weighted-hazard fit.}

\item{weights}{Numeric vector of per-row weights.}

\item{cut_times}{Numeric vector \code{c(t_1, ..., T_max)}. Used here for
its length \code{K_max} (the result is reported at each interval
index \verb{1..K_max}, position-aligned with \code{cut_times}).}
}
\value{
Numeric vector of cumulative incidence at each interval
index, length \code{K_max}.
}
\description{
Discrete-time cumulative incidence of \code{Y} computed from per-row
event indicators and weighted hazards. Single-event variant
(no competing event D), suitable for the CausalSurvival single-
outcome setting. Caller is responsible for restricting inputs to a
single standing-in arm.
}
\details{
Computes weighted hazards by interval via
\code{\link[=weighted_hazard_by_k]{weighted_hazard_by_k()}}, aligns to \code{cut_times}, and applies the
standard recursion
\deqn{F_Y(t) = \sum_{k \le t} h_Y(k) \, S(k-1)}
where \eqn{S(k-1) = \prod_{j < k} (1 - h_Y(j))}.

Ported from CausalCompetingRisks (\code{R/ipw_core.R}), un-deferred
2026-05-12 with the \code{d_event} arg dropped for the single-event
setting.
}
\keyword{internal}
