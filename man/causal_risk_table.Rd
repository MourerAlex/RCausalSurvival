% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_table.R
\name{causal_risk_table}
\alias{causal_risk_table}
\title{Risk-table counts by observed treatment arm}
\usage{
causal_risk_table(
  fit,
  count = c("at_risk", "events_y", "events_y_interval", "censored")
)
}
\arguments{
\item{fit}{A \code{"causal_survival_fit"} object from
\code{\link[=causal_survival]{causal_survival()}}.}

\item{count}{Character. One of:
\describe{
\item{\code{"at_risk"}}{Number of subjects with a row at \code{k} (still
under observation through interval \code{k}).}
\item{\code{"events_y"}}{\strong{Cumulative} count of \code{Y} events up to and
including interval \code{k} (sum of per-interval \code{y_event == 1}
from interval 1 to \code{k}). Matches ggsurvfit's \code{cum.event}
convention.}
\item{\code{"events_y_interval"}}{\strong{Per-interval} count of \code{Y}
events occurring exactly in interval \code{k} (the previous
behavior of \code{"events_y"} before v0.2; kept for hazard-density
reporting).}
\item{\code{"censored"}}{Number of subjects censored in interval
\code{k} (\code{cond_indep_cens == 1} or \code{indep_cens == 1}). Per-interval.}
}}
}
\value{
A data.frame with column \code{k} and one column per observed
treatment value, named e.g. \code{A_0} and \code{A_1}.
}
\description{
Returns counts at each cut time, grouped by the observed treatment
level (e.g., A = 0 and A = 1). Complements the cumulative
incidence curves for reporting (adjustedCurves-style risk table).
}
\details{
The user picks \strong{one} count type per call. Counts are computed
on observed subjects — there are no counterfactual rows under
v0.1.0's binary point-treatment scope.
}
\seealso{
\code{\link[=causal_survival]{causal_survival()}}, \code{\link[=plot.causal_survival_risk]{plot.causal_survival_risk()}}

Other accessors:
\code{\link[=causal_assumptions]{causal_assumptions()}},
\code{\link[=causal_contrast]{causal_contrast()}},
\code{\link[=causal_risk]{causal_risk()}}
}
\concept{accessors}
