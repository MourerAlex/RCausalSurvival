% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{fit_propensity}
\alias{fit_propensity}
\title{Fit Propensity Score Models}
\usage{
fit_propensity(
  pt_data,
  treatment,
  covariates,
  stabilize = TRUE,
  formula_full = NULL,
  formula_num = NULL
)
}
\arguments{
\item{pt_data}{Person-time data frame.}

\item{treatment}{Character. Treatment column name.}

\item{covariates}{Character vector. Baseline covariate column names.}

\item{stabilize}{Logical. If TRUE (default) also fit the marginal
numerator model A ~ 1.}

\item{formula_full}{Optional formula override for the conditional
denominator model (default: \code{A ~ L1 + L2 + ...}).}

\item{formula_num}{Optional formula override for the numerator model.
Default \code{A ~ 1} (marginal stabilization). Conditional stabilization
(e.g. \code{A ~ V_baseline}) is supported by the helper but not yet
exposed by the public API — see \code{dev/TODO.md}.}
}
\value{
List with elements:
\itemize{
\item \code{model_a} — fitted full propensity glm
\item \code{model_a_num} — fitted numerator glm, or NULL if \code{stabilize = FALSE}
\item \code{check_a} — diagnostics from \code{\link[=check_fitted_positivity]{check_fitted_positivity()}}
\item \code{check_a_num} — diagnostics for the numerator model, or NULL
}
}
\description{
Estimate the propensity score \eqn{\pi(a \mid l) = \Pr(A = a \mid L = l)}
by logistic regression. With \code{stabilize = TRUE}, also estimate the
marginal model \eqn{\pi(a) = \Pr(A = a)} for stabilized weights
(Robins & Hernán).
}
\details{
Both fits use one row per subject: the row at \code{k = 1}. Each row
\code{k} carries \eqn{L_{k-1}}, \eqn{A_{k-1}}, and the event indicator
\eqn{Y_k} (left-edge convention: covariates and treatment apply
from the start of an interval, events occur at the end). In v1 the
treatment is point — \eqn{A_{k-1} = A_0} for all \code{k} — so the
propensity fit reduces to one row per subject at \code{k = 1}, carrying
\eqn{A_0} and \eqn{L_0} measured at \code{k = 0}.
}
\keyword{internal}
