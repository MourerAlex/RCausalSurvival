% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{.resolve_display_cuts}
\alias{.resolve_display_cuts}
\title{Resolve the \code{cut_times} plot argument to a vector of x-axis positions}
\usage{
.resolve_display_cuts(cut_times, fit_cuts)
}
\description{
Accepts:
\itemize{
\item \code{NULL}: display all of \code{c(0, fit_cuts)}.
\item single positive integer \code{N}: display \code{c(0, fit_cuts[idx])} with
\code{idx <- round(seq(1, length(fit_cuts), length.out = N))}.
\item numeric vector (length >= 2): explicit subset, must be a subset of
\code{c(0, fit_cuts)}; \code{0} is auto-prepended if absent (so the baseline
column is always shown).
}
}
\details{
A length-1 numeric is always interpreted as a count, not a subset.
To display a single specific cut time, pair it with 0, e.g.
\code{cut_times = c(0, 5)}.
}
\keyword{internal}
