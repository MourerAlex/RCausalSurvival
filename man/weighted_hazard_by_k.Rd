% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazards.R
\name{weighted_hazard_by_k}
\alias{weighted_hazard_by_k}
\title{Weighted Hazard by Interval (Weighted Ratio)}
\usage{
weighted_hazard_by_k(event, k, weights)
}
\arguments{
\item{event}{Numeric vector. Event indicator at each row: 1 = event,
0 = at risk no event, NA = no longer at risk.}

\item{k}{Vector of interval indices, same length as \code{event}.}

\item{weights}{Numeric vector of per-row weights, same length.}
}
\value{
Named numeric vector: weighted hazard at each unique value
of \code{k}, names are the k values as character.
}
\description{
Per-interval weighted incidence: numerator is the weighted count of
events at each k, denominator is the weighted count at risk at each
k. At-risk rows are those with non-NA \code{event} (the convention is
that NA means the subject is no longer at risk for this event type
at this k). Framework-agnostic vector primitive used by the
\code{ipw_estimator = "km"} survival estimator.
}
\details{
Ported from CausalCompetingRisks (\code{R/ipw_core.R}), un-deferred
2026-05-12 to support \code{fit_ipw_km()}.
}
\keyword{internal}
