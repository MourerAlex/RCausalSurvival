% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{.render_summary_contrasts}
\alias{.render_summary_contrasts}
\title{Render the contrasts section: per-row table + proportional CI bar}
\usage{
.render_summary_contrasts(ctr, t_at, ci, scale)
}
\description{
Each contrast row prints: a header line with \code{RD/RR}, formula on the
chosen scale, and the null reference; an estimate-and-CI line; then a
proportional bar where \code{[} and \verb{]} mark the CI endpoints, \code{|} marks
the null, \code{*} marks the point estimate (\verb{<>} when null = estimate).
The bar's display range spans \verb{[min(lower, null), max(upper, null)]},
so the null is always visible even when it falls outside the CI.
}
\keyword{internal}
