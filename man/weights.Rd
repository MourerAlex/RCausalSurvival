% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{weights}
\alias{weights}
\title{Inverse-Probability Weights (IPW)}
\description{
Helpers for constructing IPW weights from fitted models.
}
\details{
\itemize{
\item \code{\link[=ipw]{ipw()}}            — generic core: per-row inverse of cumulative
probability of the observed history (cumprod
baked in). Used for time-varying mechanisms.
\item \code{\link[=ipw_cens]{ipw_cens()}}       — censoring weights (IPCW), routes through
\code{\link[=ipw]{ipw()}}.
\item \code{\link[=ipw_static_trt]{ipw_static_trt()}} — point-treatment weights (per-subject scalar
broadcast across the subject's rows).
}

\strong{Output convention (read before adding new helpers):} every
function in this file returns a \emph{row-ready} numeric vector of
length \code{nrow(pt_data)}, suitable for direct use as \code{glm(weights = .)}.
Combine multiple weight vectors by row-wise multiplication, NOT
cumprod — cumulative logic is already baked into the producer.

Stabilization is structural (two-cumprod ratio, Robins/Hernán form).
Truncation lives downstream in \code{\link[=apply_weight_truncation]{apply_weight_truncation()}} and is
applied to the final combined IPW weight (Cole & Hernán 2008 AJE),
not to each component.
}
\keyword{internal}
