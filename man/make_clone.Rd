% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_clone}
\alias{make_clone}
\title{Clone Baseline Across Interval Indices}
\usage{
make_clone(baseline, cut_times, treatment_col, a)
}
\arguments{
\item{baseline}{data.frame. One row per subject (typically
\code{pt_data[pt_data$k == 1, ]}).}

\item{cut_times}{Numeric vector of interval-end times \verb{t_1, ..., T_max}.
Used here only for its length \code{K_max}.}

\item{treatment_col}{Character. Treatment column name.}

\item{a}{Numeric (0 or 1). Counterfactual treatment value.}
}
\value{
data.frame with \code{nrow(baseline) * K_max} rows.
}
\description{
Broadcast each subject's baseline row across all \code{K_max} interval
indices, setting the treatment column to a fixed value \code{a}. Used by
the g-formula and IPW-MSM workers to predict counterfactual hazards
at every (subject, k) regardless of the subject's observed
event/censoring time.
}
\details{
The \code{k} column on the clone holds the integer interval index
(\verb{1..K_max}), matching the fit-time encoding of the Y-hazard / Y-MSM
model. The time grid \code{cut_times} is preserved on the calling side
(e.g. via \code{attr(pt_data, "cut_times")}) for report-time alignment.
}
\keyword{internal}
