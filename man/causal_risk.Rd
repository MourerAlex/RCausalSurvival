% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_risk.R
\name{causal_risk}
\alias{causal_risk}
\title{Cumulative incidence / survival under each arm}
\usage{
causal_risk(fit, scale = c("incidence", "survival"), ci = NULL)
}
\arguments{
\item{fit}{A \code{"causal_survival_fit"} object from
\code{\link[=causal_survival]{causal_survival()}}.}

\item{scale}{One of \code{"incidence"} (default, returns
\eqn{F^a(k)}) or \code{"survival"} (returns \eqn{S^a(k)}).}

\item{ci}{Optional. A \code{"causal_survival_bootstrap"} object from
\code{\link[=bootstrap]{bootstrap()}}. When provided, the \code{lower} / \code{upper} columns of
\verb{$risk} are populated; otherwise they are \code{NA_real_}.}
}
\value{
An S3 object of class \code{"causal_survival_risk"} with:
\describe{
\item{risk}{Long-format data.frame with columns \code{method},
\code{treatment}, \code{k}, \code{time}, \code{value}, \code{lower}, \code{upper}. One row
per \verb{(method, treatment, k)} triple.}
\item{scale}{The selected scale.}
\item{replicates, alpha}{Carried from \code{ci} for per-contrast
bands in \code{\link[=plot.causal_survival_risk]{plot.causal_survival_risk()}} (or \code{NULL}).}
\item{pt_data, id_col, treatment_col, cut_times}{References to
the fit, used by the plot method's optional risk-table panel.}
}
}
\description{
Extract the counterfactual cumulative incidence
\eqn{F^a(k) = E[Y_k^{a, c = 0}]} (or the survival
\eqn{S^a(k) = 1 - F^a(k)}) under each level \code{a} of the binary
treatment, indexed by the reporting grid \code{fit$cut_times}. Optionally
folds bootstrap confidence bands into the same table.
}
\seealso{
\code{\link[=causal_contrast]{causal_contrast()}}, \code{\link[=plot.causal_survival_risk]{plot.causal_survival_risk()}}

Other accessors:
\code{\link[=causal_assumptions]{causal_assumptions()}},
\code{\link[=causal_contrast]{causal_contrast()}},
\code{\link[=causal_risk_table]{causal_risk_table()}}
}
\concept{accessors}
