% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{ipw}
\alias{ipw}
\title{Inverse-Probability Weight (Generic Core)}
\usage{
ipw(prob_denom, id, prob_num = NULL)
}
\arguments{
\item{prob_denom}{Numeric vector in (0, 1]. P(observed history | full
conditioning), one entry per row of the person-time data. Zeros
indicate positivity violation — diagnose upstream.}

\item{id}{Long-format subject id, \strong{same length as \code{prob_denom}} (one
entry per person-time row, repeated within subject). Used to group
the cumprod within subject.}

\item{prob_num}{Optional numeric vector in (0, 1], same length as
\code{prob_denom}, or NULL. P(observed history | reduced conditioning).
NULL = unstabilized.}
}
\value{
Numeric weight vector, length \code{length(prob_denom)}.
}
\description{
Per-row inverse of the cumulative probability of the observed history:
}
\details{
W_{i,k} = prod_{j <= k} prob_num_{ij} / prod_{j <= k} prob_denom_{ij}

With \code{prob_num = NULL} the numerator collapses to 1 (unstabilized form).
Caller is responsible for constructing the per-row probabilities; this
function is agnostic to which causal mechanism is being weighted.

Cole SR, Hernán MA (2008) AJE; Hernán & Robins, \emph{Causal Inference: What
If} (ch. 12, 17).
}
\keyword{internal}
