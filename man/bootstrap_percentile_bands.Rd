% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_percentile_bands}
\alias{bootstrap_percentile_bands}
\title{Percentile confidence bands from bootstrap replicates}
\usage{
bootstrap_percentile_bands(replicates, alpha, by = c("treatment", "k"))
}
\arguments{
\item{replicates}{Long data.frame with a \code{value} column and the
grouping columns named in \code{by}.}

\item{alpha}{Two-sided significance level in \verb{(0, 1)}; bands are the
\code{alpha/2} and \code{1 - alpha/2} percentiles.}

\item{by}{Grouping column names. Default \code{c("treatment", "k")} (CS);
CCR passes \code{c("method", "arm", "k")}.}
}
\value{
Data.frame: the \code{by} columns plus \code{lower}, \code{upper}. Zero rows
(correct columns) when \code{replicates} is empty.
}
\description{
Groups the long-format \code{replicates} table by \code{by} and, within each
group, takes the lower and upper percentiles of the \code{value} column.
Returns a single long data.frame: the \code{by} columns plus \code{lower} and
\code{upper}.
}
\keyword{internal}
