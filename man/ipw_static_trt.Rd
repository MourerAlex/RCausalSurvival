% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{ipw_static_trt}
\alias{ipw_static_trt}
\title{Inverse-Probability Weight for Static (Point) Treatment}
\usage{
ipw_static_trt(
  model_full,
  pt_data,
  treatment_col,
  id_col,
  time_col = "k",
  model_num = NULL
)
}
\arguments{
\item{model_full}{Fitted glm of A on full baseline conditioning set.}

\item{pt_data}{Person-time data frame.}

\item{treatment_col}{Character. Treatment column name. Values must
be in \verb{\{0, 1\}} (standardized upstream in \code{\link[=to_person_time]{to_person_time()}}).}

\item{id_col}{Character. Subject id column name.}

\item{time_col}{Character. Interval index column name (default "k").}

\item{model_num}{Optional glm of A with reduced conditioning, for
stabilization. NULL = unstabilized.}
}
\value{
Numeric weight vector, length \code{nrow(pt_data)}. Constant
within subject (broadcast from baseline).
}
\description{
IPT weights at baseline only.
}
\details{
Predicts P(A = 1 | L) on baseline rows only (efficient: n_subjects
predictions, not n_rows), picks P(A_obs | L) per subject, inverts
(or takes the stabilized ratio with a numerator model), then
broadcasts to the long-format pt_data via subject id.

Stabilization (Robins/Hernán form): provide \code{model_num} fit with
reduced conditioning, e.g. \code{A ~ 1} (marginal stabilization).
}
\keyword{internal}
