% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.causal_survival_risk}
\alias{plot.causal_survival_risk}
\title{Plot cumulative incidence / survival curves}
\usage{
\method{plot}{causal_survival_risk}(
  x,
  arms = NULL,
  arm_colors = NULL,
  arm_labels = NULL,
  arm_linetypes = NULL,
  curves = TRUE,
  risk_table = NULL,
  risk_table_height = 0.23,
  cut_times = NULL,
  title = NULL,
  subtitle = NULL,
  x_label = "Time",
  y_label = NULL,
  base_size = 11,
  linewidth = 0.8,
  ribbon_alpha = 0.15,
  ...
)
}
\arguments{
\item{x}{A \code{"causal_survival_risk"} object from \code{\link[=causal_risk]{causal_risk()}}.}

\item{arms}{Numeric vector. Subset of \code{fit$treatment_levels} to
draw. \code{NULL} (default) draws all levels.}

\item{arm_colors}{Named character vector overriding arm hex
colors. Names must be the character representation of treatment
levels. Defaults to Okabe-Ito via \code{\link[=default_arm_palette]{default_arm_palette()}}; only
the arms you pass are overridden.}

\item{arm_labels}{Named character vector overriding the legend
labels for each arm. Defaults to \code{c("0" = "Control", "1" = "Treated")} when the levels are \code{c(0, 1)}; otherwise the level
value is used directly.}

\item{arm_linetypes}{Named character vector overriding the linetype
for each arm. Defaults to \code{c("solid", "longdash")} via
\code{\link[=default_arm_linetypes]{default_arm_linetypes()}} so curves remain distinguishable in B&W
or colorblind viewing; only the arms you pass are overridden.}

\item{curves}{Logical. \code{TRUE} (default) draws the survival /
incidence curves panel; \code{FALSE} suppresses the curves panel
entirely so only the requested \code{risk_table} panel(s) render
(table-only mode). Requires a non-NULL \code{risk_table}.}

\item{risk_table}{NULL (default — no risk-table panel) or a
character vector of one or more entries in \code{c("at_risk", "events_y", "events_y_interval", "censored")}. When a single string, one
labelled count-per-(arm, cut_time) panel renders below the curves
panel. When a vector, one panel per requested count renders stacked
in the supplied order. \code{"events_y"} is the ggsurvfit-style
cumulative Y-events count; \code{"events_y_interval"} is the
per-interval count.}

\item{risk_table_height}{Numeric. Per-panel height ratio relative to
the curves panel (which is \code{1}). When \code{risk_table} is a vector of
length \code{N}, the total bottom area is \code{N * risk_table_height}.
Default \code{0.23}.}

\item{cut_times}{Controls the x-axis tick positions on both the curves
panel and the risk-table panel (when shown). \code{0} is always included
(auto-prepended if the user forgets it).
\describe{
\item{\code{NULL} (default)}{Display \code{c(0, fit$cut_times)}.}
\item{Single positive integer \code{N}}{Display \code{c(0, ...)} where
\code{...} is \code{N} indices equidistant along \code{fit$cut_times}.}
\item{Numeric vector (length >= 2)}{Explicit subset, values must
be in \code{c(0, fit$cut_times)}. \code{0} is auto-prepended if absent.
Note: a length-1 numeric is interpreted as a count (above), not
a subset — to display a single specific cut time, pair it with
0, e.g. \code{cut_times = c(0, 5)}.}
}}

\item{title, subtitle}{Character or NULL. Plot title / subtitle.}

\item{x_label, y_label}{Axis labels. Defaults reflect the selected
\code{scale} (incidence vs survival).}

\item{base_size}{Numeric. Base font size for \code{theme_minimal()}.}

\item{linewidth}{Numeric. Width of the step lines. Default 0.8.}

\item{ribbon_alpha}{Numeric in \verb{[0, 1]}. Transparency of the CI
ribbons. Default 0.15.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot2 object.
}
\description{
Plots the counterfactual curve under each arm of the binary
treatment as a step function on the reporting grid \code{fit$cut_times},
with an optional bootstrap CI ribbon (step-transformed to match the
curve). Pairs with \code{\link[=causal_risk]{causal_risk()}} — pass a \code{"causal_survival_risk"}
object.
}
\seealso{
Other plot:
\code{\link[=plot.causal_survival_contrast]{plot.causal_survival_contrast()}}
}
\concept{plot}
