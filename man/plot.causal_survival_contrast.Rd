% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.causal_survival_contrast}
\alias{plot.causal_survival_contrast}
\title{Forest plot of counterfactual contrasts}
\usage{
\method{plot}{causal_survival_contrast}(x, base_size = 11, ...)
}
\arguments{
\item{x}{A \code{"causal_survival_contrast"} object from
\code{\link[=causal_contrast]{causal_contrast()}}.}

\item{base_size}{Numeric. Base font size for \code{theme_minimal()}.
Default \code{11}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot}/\code{patchwork} object.
}
\description{
Renders the contrast object as a horizontal forest plot, one row per
\verb{(method, contrast)} pair, with the point estimate, confidence
interval, and a vertical dashed reference line at the null value
(0 for difference, 1 for ratio).
}
\details{
Two side-by-side panels — RD on a linear x-axis, RR on a log x-axis
— so that protective and harmful ratio effects appear equidistant
from the null (forest-plot convention since Cochrane meta-analyses).
Marker size is proportional to precision (\code{1 / (upper - lower)}) so
tighter CIs receive larger squares; rows with no bootstrap
(\code{lower = NA}) render as a point only.

Per the lean-API design: the function expects the contrast object
to be method-scoped already (one method's rows; the typical case
since \code{causal_survival()} fits one method at a time). When multiple
methods are present the forest still renders one row per method,
but the design target is one-method-at-a-time.
}
\seealso{
Other plot:
\code{\link[=plot.causal_survival_risk]{plot.causal_survival_risk()}}
}
\concept{plot}
