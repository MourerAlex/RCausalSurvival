% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{.validate_bootstrap_args}
\alias{.validate_bootstrap_args}
\title{Validate and canonicalize \code{bootstrap()} arguments}
\usage{
.validate_bootstrap_args(fit, n_boot, alpha, seed)
}
\description{
Checks \code{fit} class, the integer-positivity of \code{n_boot}, the
\verb{(0, 1)} range of \code{alpha}, sets the RNG seed when supplied, and
verifies \code{fit$pt_data} is present (refit with \code{keep_data = TRUE},
or attach manually via \code{to_person_time()} — the error message gives
a copy-pasteable reconstruction snippet). Also cross-checks that
the attached \code{pt_data}'s cut grid, \code{treatment_levels}, and
\code{treatment_col} match the corresponding fields on \code{fit} — three
silent-failure paths (wrong replicate alignment, swapped arms,
wrong column dispatched) that aren't caught by R's normal errors.
Returns a canonical args list with \code{fit}, \code{n_boot}, \code{alpha},
\code{pt_data}, \code{id_col}, and \code{method}.
}
\keyword{internal}
