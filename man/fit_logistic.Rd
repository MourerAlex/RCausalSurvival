% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazards.R
\name{fit_logistic}
\alias{fit_logistic}
\title{Fit a Logistic GLM with Warning Capture and Positivity Check}
\usage{
fit_logistic(formula, data, label, weights = NULL)
}
\arguments{
\item{formula}{A fitted model formula.}

\item{data}{Data frame passed to \code{glm()}.}

\item{label}{Human-readable label for warnings and diagnostics.}

\item{weights}{Optional numeric vector of \code{glm} case weights, length
\code{nrow(data)}. NULL (default) fits an unweighted glm. When supplied,
the vector is stashed on \code{data} under an internal column name
(\code{.fit_logistic_w_}) and passed to \code{glm()} by that name; this
side-steps NSE on the \code{weights} formal.}
}
\value{
A list with \code{model} (the glm object) and \code{check} (diagnostics).
}
\description{
Internal helper. Fits a binomial logistic glm, captures any \code{glm()}
warnings via \code{withCallingHandlers()}, re-emits them so the calling
fitter's collector grabs them, and builds a diagnostics list via
\code{\link[=check_fitted_positivity]{check_fitted_positivity()}}.
}
\details{
Used by \code{\link[=fit_hazard_models]{fit_hazard_models()}} (Y, C hazards on person-time), by
\code{\link[=fit_propensity]{fit_propensity()}} (treatment model on baseline rows), and by the IPW
worker for the weighted Y-MSM fit.
}
\keyword{internal}
