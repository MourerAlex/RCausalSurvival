% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_survival.R
\name{.with_collected_warnings}
\alias{.with_collected_warnings}
\title{Collect glm warnings from a fit expression}
\usage{
.with_collected_warnings(expr, context = "causal_survival()")
}
\description{
Evaluates \code{expr} under a calling handler that muffles \code{warning()} calls
into a vector and re-emits a single grouped notice at the end. Returns
\code{expr}'s value with \verb{$warnings} attached. Used by \code{\link[=causal_survival]{causal_survival()}} so
inner-fitter warnings surface as \code{fit$warnings} rather than streaming
mid-run.
}
\details{
Works because R promises evaluate \code{expr} lazily — the expression is
forced inside the \code{withCallingHandlers} frame, so handlers are active
when the workers run.
}
\keyword{internal}
