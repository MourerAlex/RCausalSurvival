% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_contrast.R
\name{causal_contrast}
\alias{causal_contrast}
\title{Counterfactual contrasts on the reporting grid}
\usage{
causal_contrast(
  fit,
  reference = NULL,
  contrasts = NULL,
  ci = NULL,
  time = NULL,
  scale = c("incidence", "survival")
)
}
\arguments{
\item{fit}{A \code{"causal_survival_fit"} object from
\code{\link[=causal_survival]{causal_survival()}}.}

\item{reference}{Optional reference arm (one of the levels in
\code{fit$treatment_levels}). \code{NULL} defaults to the lowest level.}

\item{contrasts}{Optional named list of custom contrasts (deferred
to v2 multi-arm; see \code{dev/unused_code/resolve_contrast_pairs_v2.R}).
Must be \code{NULL} in v0.1.0.}

\item{ci}{Optional. A \code{"causal_survival_bootstrap"} object from
\code{\link[=bootstrap]{bootstrap()}}. When \code{NULL}, point estimates only are returned
with \code{lower} / \code{upper} set to \code{NA_real_}. A loud \code{warning()} is
emitted in that case: contrasts without confidence intervals are
not meaningful and their interpretation is strongly discouraged.}

\item{time}{\code{NULL} (default, resolves to the final cut time per
spec §3.4) or a numeric scalar resolved on the reporting grid
via \code{\link[=snap_time]{snap_time()}}. Out-of-bounds values raise an error.}

\item{scale}{One of \code{"incidence"} (default — RD and RR computed on
\code{F^a(k)}) or \code{"survival"} (RD and RR computed on
\code{S^a(k) = 1 - F^a(k)}). The null reference values (\code{0} for RD,
\code{1} for RR) are the same on both scales; only the magnitudes
(and the sign of RD) change. RD on survival is the sign-flipped
RD on incidence; RR on survival is a different ratio entirely.}
}
\value{
An S3 object of class \code{"causal_survival_contrast"} with:
\describe{
\item{contrasts}{Long-format data.frame with columns \code{method},
\code{name}, \code{treatment_a}, \code{treatment_b}, \code{contrast}, \code{k}, \code{time},
\code{estimate}, \code{lower}, \code{upper}.}
\item{method}{The fit's estimation method.}
\item{alpha}{Bootstrap significance level (or \code{NULL}).}
\item{time}{The resolved cut time.}
\item{scale}{Either \code{"incidence"} or \code{"survival"}.}
}
}
\description{
Compute pairwise contrasts of the counterfactual cumulative
incidence between treatment arms on the reporting grid. The risk
difference \verb{RD(k) = F^\{a\}(k) - F^\{a'\}(k)} (operator \code{"-"}) and the
risk ratio \verb{RR(k) = F^\{a\}(k) / F^\{a'\}(k)} (operator \code{"/"}) are the
two default operators.
}
\details{
Under the v0.1.0 binary point-treatment scope there is a single
arm pair \verb{(max, min)} of \code{fit$treatment_levels}. The \code{reference} /
\code{contrasts} arguments are carried in the signature for forward
compatibility with multi-arm (v2) and separable-effects (downstream
package) scopes; in v0.1.0 supplying a non-\code{NULL} \code{contrasts}
raises an error.
}
\seealso{
\code{\link[=causal_risk]{causal_risk()}}, \code{\link[=plot.causal_survival_contrast]{plot.causal_survival_contrast()}},
\code{\link[=bootstrap]{bootstrap()}}

Other accessors:
\code{\link[=causal_assumptions]{causal_assumptions()}},
\code{\link[=causal_risk]{causal_risk()}},
\code{\link[=causal_risk_table]{causal_risk_table()}}
}
\concept{accessors}
