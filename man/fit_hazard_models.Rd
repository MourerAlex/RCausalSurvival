% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazards.R
\name{fit_hazard_models}
\alias{fit_hazard_models}
\title{Fit Hazard Models}
\usage{
fit_hazard_models(
  pt_data,
  treatment,
  covariates,
  active_methods,
  formulas,
  ipcw = TRUE
)
}
\arguments{
\item{pt_data}{Data frame in person-time format.}

\item{treatment}{Character. Treatment column name. Used directly in both
Y- and C-hazard formulas (no working-copy column).}

\item{covariates}{Character vector. Covariate column names.}

\item{active_methods}{Character vector. Subset of
\code{c("gformula", "ipw")}. Determines which models get fit:
\itemize{
\item \code{"gformula"}: model_y only
\item \code{"ipw"}:       model_c (only when \code{ipcw = TRUE})
}}

\item{formulas}{Named list or NULL. User-specified formulas (names \code{y},
\code{c}). Any entry absent falls back to the default.}

\item{ipcw}{Logical. When FALSE, the censoring model is not fit and
\code{model_c} stays NULL.}
}
\value{
A list with two entries:
\describe{
\item{models}{Named list: \code{model_y}, \code{model_c} (glm objects or NULL).}
\item{checks}{Named list: \code{y}, \code{c} (per-model diagnostics or NULL).}
}
}
\description{
Fits pooled logistic regression models for the Y-hazard and (optionally)
censoring hazard on person-time data. Per-model diagnostics are collected
via \code{\link[=check_fitted_positivity]{check_fitted_positivity()}}.
}
\details{
Default formula: \verb{<event> ~ treatment + k + I(k^2) + I(k^3) + covariates}
(additive, no interactions). The integer-\code{k} polynomial in time follows
the LOCKED \verb{(0, t_1], ..., (t_\{K_max-1\}, T_max]} convention from spec
§3.0.2 — \code{k} is the interval index, not its left-edge time.

Fit populations:
\itemize{
\item Y-hazard (\code{y_event ~ ...}): rows with \code{indep_cens == 0 & cond_indep_cens == 0}.
\item C-hazard (\code{cond_indep_cens ~ ...}): rows with \code{indep_cens == 0}.
}

The Y-hazard model fit here is \strong{unweighted}.
}
\keyword{internal}
